/** Names the candidates, because the fix is either adding one of them or passing `entry`. */
export const NO_ENTRY_MESSAGE =
  'No entry point found. Expected one of src/main.tsx, src/main.ts, src/index.tsx — or pass `entry`.';
